<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.prisma</groupId>
  <artifactId>processhub-bpmn-manipulation</artifactId>
  <packaging>jar</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>processhub-bpmn-manipulation</name>

  <url>http://maven.apache.org</url>

  <properties>
    <camunda-bpmn-model.version>7.3.0</camunda-bpmn-model.version>
    <junit.version>4.12</junit.version>
  </properties>

  <dependencies>
    <!-- Junit Tests -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>

    <!-- Camunda BPMN -->
    <dependency>
      <groupId>org.camunda.bpm.model</groupId>
      <artifactId>camunda-bpmn-model</artifactId>
      <version>${camunda-bpmn-model.version}</version>
    </dependency>
  </dependencies>
</project>
